public interface IActivateable {
    
    /**
     * everything that can be activated, player cant interact with this, but an interactable can
     * @param nothing
     * @return nothing
     */
    void Activate();
}
